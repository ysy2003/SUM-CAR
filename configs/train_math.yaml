base_model: meta-llama/Llama-3.2-1B
mem:
  num_slots: 4096  # Drastically reduced
  k_top: 16  # Reduced k_top
  alpha: 1.0
  tau: 10.0
  use_gate: true
  normalize_retrieval: true
train:
  epochs: 3
  lr: 5.0e-4
  lr_kv: 1.0e-3
  lr_gate: 1.0e-4
  batch_size: 2  # Reduced
  max_length: 256  # Reduced
  probe_steps: 500
  top_t: 512  # Much smaller
  refresh_every: 100
  save_dir: out/math
  seed: 42
  dataset: gsm8k
  probe_steps: 1000
  top_t: 2048
  save_dir: out/math
  seed: 42
  dataset: gsm8k
