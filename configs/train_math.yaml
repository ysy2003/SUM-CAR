base_model: meta-llama/Llama-3.2-1B
mem:
  num_slots: 8192  # Conservative for v3-32
  k_top: 16
  alpha: 1.0
  tau: 10.0
  use_gate: true
  normalize_retrieval: true
train:
  epochs: 3
  lr: 5.0e-4
  lr_kv: 1.0e-3
  lr_gate: 1.0e-4
  batch_size: 2  # Smaller batch
  max_length: 256  # Reduced
  probe_steps: 500
  top_t: 1024  # Reduced
  refresh_every: 100
  save_dir: out/math
  seed: 42
  dataset: gsm8k
  probe_steps: 1000
  top_t: 2048
  save_dir: out/math
  seed: 42
  dataset: gsm8k
